# M27 — R14/R15 Source (Ghidra Task)

只做一件事：

在 FUN_1816c2a20 中，找到 R14 和 R15 来自哪里。

## 背景

FUN_1816c2a20 内部循环：
```
R14 = start (当前节点)
R15 = end   (结束节点)
步长: 0x2d8
循环: R14 != R15
```

## 任务

在 Ghidra 中打开 FUN_1816c2a20，确认 R14 和 R15 的值是：

A. 从参数传入的（哪个参数？）
B. 从 a1 结构体里读出来的（哪个偏移？）
C. 从全局变量读的

## 输出格式

一句话就行。例如：
```
R14 = param_2->start
R15 = param_2->end
```
或：
```
R14 = a1 + 0x50
R15 = a1 + 0x58
```
