# M24.5 — Message Manager Validation 分析报告

## 基本信息

| 项目 | 值 |
|------|-----|
| DLL | Weixin.dll 4.1.10.29 |
| 目标 | 验证 FUN_181771eb0 → GetMessageListBySvrIds → MessageNode 链路 |
| 状态 | **链路已确认 ✅** |

## 已确认的完整调用链

```
FUN_181771eb0  (DLL +0x01771eb0)  ← 消息管理事件入口
    │  param_1 = 会话/对话对象
    │    ├── +0x08: 消息链表头
    │    ├── +0x48: 消息管理器对象
    │    └── ...
    │
    ├── FUN_181493b10  — 构建查询参数
    │
    ├── FUN_1816f3b30(GetMessageListBySvrIds, DLL +0x016f3b30)
    │   │  参数: (消息管理器, 查询参数, 链表, 结果标记)
    │   │
    │   ├── [链表遍历] 搜索已有消息
    │   │   └── ★ FUN_1816c2a20 — 0x2d8 步长遍历/过滤
    │   │
    │   ├── [0x2d8 数组遍历]
    │   │   └── ★ FUN_1816f3510 — 创建 0x2f0 缓存节点, 插入哈希表
    │   │
    │   └── 返回结果到哈希表容器
    │
    ├── [遍历结果, 分配 0x2e8 回调包装]
    │   └── FUN_181729f20 — 分发到上层回调
    │
    └── [清理] 释放 0x2f0 哈希表缓存节点
```

## Task 分析结果

### Task 1: param_1 对象布局

| 偏移 | 用途 | 说明 |
|------|------|------|
| +0x00 | vtable? | 对象头部（可能是虚表指针） |
| +0x08 | 消息链表头 | 传给 GetMessageListBySvrIds 作为输入/输出容器 |
| +0x48 | 消息管理器 | 传给 GetMessageListBySvrIds 作为 arg1 |

param_1 是一个**会话消息对象**（Conversation/Session），封装了消息管理器和消息链表。包含字符串特征：message, msg, history, svrid 相关。

### Task 2: GetMessageListBySvrIds 参数

| 序号 | 参数 | 来源 | 用途 |
|------|------|------|------|
| arg1 (RCX) | `local_68` | param_1 + 0x48 | 消息管理器 |
| arg2 (RDX) | `&local_d8` | 局部变量, 通过 FUN_181493b10 构建 | **查询条件** |
| arg3 (R8) | `param_1 + 8` | 从 param_1 读取 | **消息链表 (输入+输出)** |
| arg4 (R9) | `&local_178` | 局部变量 | **结果标记** |

### Task 3: 查询语义 → A

**A. 根据多个 svrid 查询消息** ✅

- 函数名明确：`"GetMessageListBySvrIds"`
- 通过 FUN_181493b10 构建查询参数对象
- 遍历哈希缓存查找匹配的 svrid

### Task 4: 结果容器

结果存储在**自定义哈希表容器**中：

| 项目 | 细节 |
|------|------|
| 容器类型 | 自定义哈希表 (链地址法) |
| 节点大小 | **0x2f0** 字节 |
| 管理函数 | FUN_1816f3510 (创建/插入/查找) |
| 哈希算法 | FNV-1a (64-bit) |
| 清理方式 | 函数尾部遍历释放所有 0x2f0 节点 |

## 所有定位的函数汇总

| 函数 | DLL 偏移 | 角色 | 深入分析 |
|------|---------|------|---------|
| `FUN_1816ff6b0` | +0x016ff6b0 | GetPagedMessages (入口) | ✅ |
| `FUN_1816fa3f0` | +0x016fa3f0 | 构建搜索查询参数 | ✅ |
| `FUN_1816fcc60` | +0x016fcc60 | 构建查询参数 | ✅ |
| `FUN_1816c2a20` | +0x016c2a20 | 0x2d8 消息过滤/遍历核心 | ✅ |
| `FUN_1816c2b30` | +0x016c2b30 | CheckMessageLiveStatus | ✅ |
| `FUN_1816f3b30` | +0x016f3b30 | GetMessageListBySvrIds | ✅ |
| `FUN_1816f3510` | +0x016f3510 | 0x2f0 消息缓存 (哈希表) | ✅ |
| `FUN_181771eb0` | +0x01771eb0 | 消息管理事件入口 | ✅ |

## 下一步建议

按任务要求，链路已确认，建议：

1. **停止逆向** — 已经到达消息管理层
2. **进入导出器开发** — 基于已知的函数地址和结构体步长 (0x2d8) 开发运行时导出器
3. **如果仍需找 0x2d8 Creator** — 需要追踪网络接收层 (跨模块 FUN_185b91d80)，但不在本次目标范围内
