# M23 — MessageNode Creator Hunt 分析报告

## 基本信息

| 项目 | 值 |
|------|-----|
| DLL | Weixin.dll 4.1.10.29 |
| 目标 | 找到创建/填充 0x2d8 MessageNode 的函数 |
| MessageNode 步长 | 0x2d8 (728) 字节 |
| 已知字段偏移 | +0x120(receiver), +0x268(content_ptr), +0x288(content_ptr2) |

## 分析发现

### 1. FUN_1816f3b30 — GetMessageListBySvrIds

| 项目 | 值 |
|------|-----|
| 地址 | `0x1816f3b30` |
| DLL 偏移 | `0x016f3b30` |
| 调用方 | 4 个 (含跨模块调用) |
| 字符串标记 | `"GetMessageListBySvrIds"` |

**内部结构：**

```
FUN_1816f3b30
├── [链表遍历 1] 每个节点:
│   ├── FUN_1816ca380   — 消息处理
│   ├── ★ FUN_1816c2a20 — 消息过滤 (1816f410c)
│   ├── FUN_180a537b0   — 容器操作
│   ├── FUN_1816ef920   — 消息处理
│   ├── FUN_1816c8300   — 消息处理
│   └── FUN_1816cc4c0   — 消息处理
│
├── [链表遍历 2] 同模式 (第二次调用 FUN_1816c2a20 at 1816f41bc)
│
├── [0x2d8 数组遍历 1] 步进 +0x2d8:
│   ├── 读 [R15+0x148] 字段
│   └── ★ FUN_1816f3510 — 消息缓存操作 (1816f4277)
│
└── [0x2d8 数组遍历 2] 步进 +0x2d8:
    ├── 读 [R14+0x148] 字段
    └── ★ FUN_1816f3510 — 消息缓存操作 (1816f42e7)
```

### 2. FUN_1816f3510 — Message Cache (不是 Creator)

| 项目 | 值 |
|------|-----|
| 地址 | `0x1816f3510` |
| DLL 偏移 | `0x016f3510` |
| 调用方 | 6 个 |

**核心逻辑：**

1. **分配 0x2f0 字节** — `MOV ECX, 0x2f0; CALL FUN_18680e0fc`
2. **存消息 ID** — `MOV [RBX+0x10], [R15]`
3. **从 0x2d8 结构体拷贝数据** — `CALL FUN_181482400` (源: R14/R9 指向的 0x2d8 元素)
4. **FNV-1a 哈希表查找** — 判断是否已缓存
5. **未缓存则插入哈希表** — 双向链表插入操作
6. **清理** — 如果操作失败/异常，释放 0x2f0 分配

**本质：** 这是一个**消息缓存层**。它维护一个哈希表，以消息 ID 为 key，缓存 0x2f0 大小的节点。当已有缓存时直接返回，否则从 0x2d8 原始结构体拷贝数据并新插入缓存。

### 3. 0x2d8 MessageNode 创建者

**未找到。** 当前的追踪路径如下：

```
消息缓存 (0x2f0 分配)
  ↑ FUN_1816f3510
消息数组遍历
  ↑ FUN_1816c2a20 (步长 0x2d8)
消息列表管理
  ↑ FUN_1816f3b30 (GetMessageListBySvrIds)
消息列表查询
  ↑ ── ...
消息服务器请求
  ↑ ── ...
原始消息分配 (0x2d8) ← ? 在更上游
```

`0x2d8` 的分配很可能在**网络接收层** — 从服务器拉取消息后直接分配并填充。需要追踪 `FUN_1816f3b30` 的上游（特别是 `FUN_1835c4db0` 这个跨模块调用者）才能找到。

## 现状总结

| 函数 | 地址 (DLL 偏移) | 角色 | 状态 |
|------|-----------------|------|------|
| `FUN_1816ff6b0` | +0x016ff6b0 | GetPagedMessages | 已确认 |
| `FUN_1816c2a20` | +0x016c2a20 | 消息过滤/遍历 | 已分析 |
| `FUN_1816f3b30` | +0x016f3b30 | GetMessageListBySvrIds | 已分析 |
| `FUN_1816f3510` | +0x016f3510 | 消息缓存 (0x2f0) | 已分析 |
| **0x2d8 Creator** | ? | 消息结构体创建 | **未发现** |

## 建议

1. **如果 Hook 目标明确** — 现有信息已经足够定位 GetPagedMessages + FUN_1816c2a20
2. **如果一定要找 Creator** — 需要追踪跨模块调用 `FUN_1835c4db0`，这可能需要分析其他 DLL (WeChatWin.dll?)
3. **时间估计** — 找到 Creator 可能需要 1-2 小时深入追踪，且不一定有实际 Hook 价值
