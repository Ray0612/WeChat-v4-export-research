# GetPagedMessages (FUN_1816ff6b0) Call Tree 分析报告

## 基本信息

| 项目 | 值 |
|------|-----|
| 函数地址 | `0x1816ff6b0` |
| DLL 偏移 | `0x016ff6b0` |
| 版本 | Weixin.dll 4.1.10.29 |

## 函数总览

- 函数大小：约 0x3000 字节
- 内部 CALL 总数：28 个
- 子函数（去重后）：28 个

## Call Tree

```
FUN_1816ff6b0 (GetPagedMessages)
│
├── 构建搜索参数
│   ├── FUN_1816fa3f0  — 构建 {orientation, sort_order, types, sender, query_more, ...}
│   └── FUN_1816fcc60  — 构建 {create_time, sort_seq, local_id}
│
├── ★ FUN_1816c2a20  — 核心消息遍历入口
│   │  遍历 0x2d8 步长的消息数组，循环处理每条消息
│   │
│   ├── FUN_1816c2b30  — CheckMessageLiveStatus
│   │   对每条消息检查直播状态，过滤/分类
│   │   访问结构体偏移: 0x730/0x738/0x790/0x798
│   │   使用 FNV-1a hash 做哈希表查找
│   │
│   ├── FUN_1816c4100  — 引用计数转发 → FUN_183566640 (跨模块)
│   │
│   ├── FUN_1816c4230  — (未深入分析)
│   │
│   ├── FUN_1816c44d0  — 引用计数转发 → FUN_182bd8310 (跨模块)
│   │
│   └── FUN_1816c4630  — 状态跟踪/日志记录
│       写状态位到 +0x74/0xf4/0x174/0x1f4 等偏移
│       包含 base62 编码序号打印
│       访问结构体偏移: 0x148/0x278/0x280
│
├── FUN_1816c2a20 的其他子函数:
│   FUN_1816ca380, FUN_1816cc4c0, FUN_1816c8300, FUN_1816ce220
│   (在核心代码区域，未深入分析)
│
├── 运行时/库函数 (C++ CRT, 地址 18000XXXX)
│   FUN_18018ce40, FUN_1809490a0, FUN_180054470, FUN_180a537b0
│   FUN_180068740, FUN_180068870, FUN_180068bc0, FUN_180068cc0
│   FUN_180069160, FUN_18000b690, FUN_1800091f0, FUN_180082730
│   FUN_180f29ab0
│
└── 跨模块调用 (thunk)
    FUN_1835edf20, FUN_186861478, FUN_1868970d0, thunk_FUN_18686f098
    FUN_18680e0fc
```

## 分析结论

### 1. GetPagedMessages 的内部结构

GetPagedMessages 内部工作流程大致为：

1. **构建查询参数**（`FUN_1816fa3f0`/`FUN_1816fcc60`）
   - 拼接 JSON 格式的搜索条件字符串
   - 包含 orientation, sort_order, types, sender, create_time, sort_seq, local_id 等字段

2. **遍历消息数组**（`FUN_1816c2a20`）
   - 以步长 `0x2d8` 遍历结构体数组（微信消息/联系人的标准结构体大小）
   - 对每个元素调用一系列处理函数

3. **消息过滤**（`FUN_1816c2b30`）
   - 名为 `"CheckMessageLiveStatus"`，检查消息直播状态
   - 用 FNV-1a hash 做哈希表快速查找
   - 符合条件的进入下一流程，不符合的跳过

4. **状态记录**（`FUN_1816c4630`）
   - 记录每条消息的处理状态/阅读状态
   - 写状态位到结构体特定偏移

### 2. 关键候选函数 (P0)

| 优先级 | 函数 | 理由 |
|--------|------|------|
| **P0** | **FUN_1816c2a20** (偏移 `0x016c2a20`) | 消息数组遍历主入口，最可能 Hook 的精确位置 |
| P1 | FUN_1816c2b30 | 消息过滤/检查，用于理解消息结构 |
| P2 | FUN_1816fa3f0 / FUN_1816fcc60 | 查询参数构建，辅助理解调用上下文 |

### 3. 注意事项

- GetPagedMessages 本身内部不直接提取消息内容，而是将消息数据通过**输出参数**或**返回值**传递出去
- 实际的消息数据处理可能在调用方或更下游的跨模块函数中（`FUN_183566640`、`FUN_182bd8310`）
- 如果目标是 Hook 提取消息，建议在 **GetPagedMessages 返回时拦截输出参数**
- 如果目标是 Hook 消息处理逻辑，建议在 **FUN_1816c2a20** 处拦截
- 结构体步长 `0x2d8` 是微信消息结构体的标准大小，可用于解析消息数组

## 对比旧版 (4.1.9.56)

| 项目 | 4.1.9.56 | 4.1.10.29 |
|------|----------|-----------|
| GetPagedMessages 偏移 | `0x016ade70` | `0x016ff6b0` |
| 函数大小 | ~0x3000 | ~0x3000 |
| 消息结构体步长 | 0x2d8 | 0x2d8（一致） |
