# 微信 4.1.10.29 研究日志 — 2026-06-07

> 第五天：收网日 — 从 M30 到 M66，完成全量历史消息捕获与导出
> 里程碑：30 轮实验

---

## 上午：运行时捕获验证 (M30-M33)

### M30: MessageNode 指针捕获
- Hook FUN_1816c2a20 循环体，通过 R14 寄存器获取当前 MessageNode 指针
- 发现 0x2d8 结构体在循环体阶段只含元数据（msgid、msgsource），不含文本
- 确定了 a1 参数是迭代器，不是消息结构体本身

### M31: 导出器原型
- Hook Caller1 onEnter + pymem 后处理失败（0x2d8 已释放）
- Hook 循环体 R14 成功捕获 90 条元数据
- 确认 receiver 字段稳定，但文本内容不在这个处理链中

### M32: Caller1 + 循环体组合 Hook
- 16 次翻页捕获，Caller1 稳定命中
- 90 条消息元数据导出（receiver + cursor + counter）
- 文本内容仍未出现

### M33: 文本填充定位
- Hook FUN_1816c2a20 下游全部 5 个函数（c4100, c4230, c44d0, c4630, c2b30）
- 只有 FUN_1816c44d0 传了 receiver wxid
- **没有任何下游函数包含文本内容**
- 结论：文本不在这个处理链中

## 中午：反向追踪 + 缓存调查 (M34-M35)

### M34: 缓存对象调查
- Hook FUN_1816f3510（消息缓存，分配 0x2f0）
- 每次翻页约 10 次调用
- **0x2f0 对象中也没有文本内容**

### M35: 反向指针追踪
- 从已知文本 "RAY_NODE_001" 搜索反向引用
- **扫描 852MB 内存，0 个反向引用**
- 最终结论：文本是内联存储的，不通过指针引用

## 下午：紧凑结构重新发现 (M36-M38)

### M36: 🏆 紧凑结构重新发现
- 在分析 RAY_NODE_001 周围内存时，发现旧版紧凑结构（34B）的新版格式
- **新格式：39B，前缀 `XX 02 05 09 01 01 04`（旧版为 `1b 02 05 09`）**
- 导出器实现：扫描前缀 → 提取内联文本
- **首次导出 241 条消息**，包含第 1 天的 TEST_RAY、HELLO 系列等

**导出的消息样本：**
```
#329: TEST_RAY_20260605_938274615 (4天前的消息)
#331: HELLO_1
#263: https://lingdaoyi1.pages.dev/0519 (真实URL)
#297: 咱们明天晚上正常上课噢~ (真实聊天)
```

### M37: 结构验证
- 对比多条消息的二进制布局，确认结构一致性
- 确认这是完整消息记录（含序号、会话标签），不是单纯缓存

### M38: 覆盖率和准确率验证
- 同步消息测试：手机发 PHONE_ONLY_001-003，**全部命中** ✅
- 历史跨度测试："买个台灯" 在内存中存在（非紧凑格式）
- 准确率：**95%**（20 条中 19 条正确）

## 傍晚：缓存策略 + 冷启动验证 (M39-M42)

### M39: 缓存边界分析
- 142 条消息，序号范围 312~58915，中间大量断层
- 结论：紧凑结构是滑动窗口缓存，保留约 150 条最新消息

### M40: 历史存储源确认
- GetPagedMessages 6 次调用期间无任何文件操作
- 数据来源是内存（启动时预加载）

### M41: 冷启动验证
- 冷启动：0 条消息
- 打开聊天：0 条消息
- 翻页后：2 条消息
- 结论：紧凑结构是翻页时填充的显示缓存

### M42-M46: Ghidra 上游分析
- 追踪 FUN_185b91d80 → FUN_181771eb0 → FUN_1816f3b30
- 确认 0x2d8 Creator = FUN_181bc3b00（分配 6 × 0x2d8）
- 确认完整消息链路

## 夜间：全量历史捕获 (M53-M66)

### M53: 冷启动内存分析
- 对比翻页前后的内存区域变化
- 发现新区域包含 `db_storage\message\message` 路径
- **发现 Chromium LevelDB 存储的消息数据**

### M57: 实时监控 + 自动 dump
- 3 秒轮询 Weixin.exe 内存新区域
- 自动 dump 含 msgsource/wxid_ 的区域
- **捕获 162 个文件，154MB LevelDB 数据**

### M59: 全量 XML 解析
- 解析出 **6207 条 XML → 1141 条去重消息**
- 包含 content、msgsource、type、wxid
- 时间范围：2017~2026

### M61: 编码修复 + 结构化输出
- 发现中文用 GBK 编码（文件名字段），纯文本用 UTF-8
- 修复双编码支持
- 输出结构化 JSON + 可读聊天格式

### M64: 快速捕获优化
- 轮询从 3 秒改为 0.5 秒
- 捕获 86 个文件，3435 条消息
- 其中 21 条带有结构化 `fromusername`（原始聊天消息）

### M65-M66: 会话重建
- 按会话分组 + 时间戳排序 + 说话人提取
- 输出可读聊天记录格式（.md）
- 最终：**11 个会话，29 条带发送者的真实聊天记录**

---

## 当日成果总结

| 阶段 | 里程碑 | 状态 |
|------|--------|------|
| 运行时捕获 | M30-M33 | ✅ 完成 |
| 反向追踪 | M34-M35 | ✅ 文本内联结论 |
| **紧凑结构导出** | **M36** | **🏆 核心产出** |
| 准确率验证 | M37-M38 | ✅ 95% |
| 缓存策略 | M39-M41 | ✅ 理解清晰 |
| Creator 确认 | M42-M46 | ✅ FUN_181bc3b00 |
| **全量历史捕获** | **M53-M59** | **🏆 162 文件，154MB** |
| 快速捕获 | M64 | ✅ 0.5 秒轮询 |
| 会话重建 | M65-M66 | ✅ 可读聊天格式 |

### 关键文件产出

```
scripts/m36_compact_exporter.py     — 紧凑结构导出器（可用）
scripts/m59_full_parse.py           — 全量 LevelDB 解析器
scripts/m61_fix_encoding.py         — 编码修复版解析器
scripts/m64_fast_capture.py         — 快速内存捕获
scripts/m66_chat_reconstruction.py  — 会话重建

experiments/logs/m66_chat.md        — 最终可读聊天记录
experiments/logs/m66_structured.json — 结构化 JSON

reports/final_project_report.md     — 最终项目报告
reports/m61_status_to_gpt.md        — GPT 状态汇报
```

### 项目总状态

| 指标 | 数值 |
|------|------|
| 总文件 | 199 个 |
| 项目大小 | 8MB |
| 脚本数 | ~30 个 |
| 报告数 | 15+ 份 |
| 可导出消息 | M36: ~200 条近期 + M59: 1157 条历史 |
| 导出准确率 | 95% |
